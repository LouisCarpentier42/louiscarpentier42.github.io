
## Building locally

Make sure that you have ruby installed on your machine (version 3.3.11 was used here) and check if Jekyll is available:
```bash
jekyll -v
```
Install all the gems with bundle:
```bash
bundle install
``` 
Start up the local server:
```bash
bundle exec jekyll serve
```
